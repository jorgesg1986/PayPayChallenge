package com.paypay.challenge.model

/**
 * Object with Strings that are used in several places to avoid mistyping
 */

object Fields {
  val timestamp = "timestamp"
  val clientIp = "clientIp"
  val URL = "URL"
  val duration = "duration"
}
